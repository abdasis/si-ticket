@if(!empty($detail))

@endif

@if(!empty($edit))
    <a href="{{$edit}}">
        <button class="btn btn-warning btn-sm"><i class="fas fa-edit"></i></button>
    </a>
@endif

@if(!empty($editModal))

@endif

@if(!empty($delete))

@endif
